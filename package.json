{
  "name": "the-raconteurs-commonplace",
  "private": true,
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "build": "node kobo/build.mjs && npm run build:full",
    "build:kobo": "node kobo/build.mjs",
    "build:full": "npm --prefix full run build",
    "dev:full": "npm --prefix full run dev",
    "preview:full": "npm --prefix full run preview"
  }
}
